apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: oasis-ingress
  namespace: oasis
  #annotations:
    # Use appropriate annotations for your Ingress controller, e.g., for NGINX:
    # nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: oasis.minikube.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: oasis-service
            port:
              number: 8080
